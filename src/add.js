/**
 * Adds two numbers.
 *
 * @param {number} a The first number.
 * @param {number} b The second number.
 * @returns {number} Returns the sum.
 */
function add(a, b) {
    return a + b;
}

module.exports = add;
